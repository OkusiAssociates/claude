# Git Commit Rules

- **NEVER** mention "claude" or "claude code" in commit messages
- Commits must be authored as **'Biksu Okusi <biksu@okusi.id>'**, never 'Test User' or 'claude'
- Use conventional commit style when appropriate
